<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ayurvedahealthcareapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AyurvedaHealthCareApp">
        <activity android:name=".doctor_Activity"></activity>
        <activity android:name=".home_remidies_Activity" />
        <activity android:name=".calender_Activity" />
        <activity android:name=".medicine_Activity" />
        <activity android:name=".pharmacy_Activity" />
        <activity android:name=".location_Activity" />
        <activity android:name=".Dashboard" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterDoctor" />
        <activity android:name=".RegisterPatient" />
        <activity android:name=".ChoicePage" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>